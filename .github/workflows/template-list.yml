name: Template List
on:
  - push
  - pull_request

jobs:
  template-list-make:
    runs-on: ubuntu-latest
  
    steps:
      - name: Template List Generator
        uses: maael/template-repository-usage-action@v1.1.0
        with:
          token: ${{ secrets.GH_TOKEN }}
  #        repo: 'crunchy-template-master'
